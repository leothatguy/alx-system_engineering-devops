#!/usr/bin/env bash
# Display active IPv4 addresses on the machine

# Use `ip` command to list IP addresses and filter for active IPv4 ones
ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}' | sort -u
